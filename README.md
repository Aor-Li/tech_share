# TechShare

这是一个极简的技术分享报告项目。

- 在 `outline/` 规划主题、章节目标和内容主线。
- 大纲确认后，在 `content/` 中逐章迭代正文。
- 将资料和来源笔记放进 `references/`，作为写作输入。
- 代理协作流程统一维护在 `spec/README.md`。
- 将确认后的内容汇总进 `report.html`；分享时只需发送该文件，也可以在浏览器中打印为 PDF。

无需安装依赖、启动服务或执行构建命令。
