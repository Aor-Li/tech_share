# Presentation 工作区

这里保存由已确认正文生成的展示网页及其版本历史。生成时机、版本规则和验收标准见 [`spec/README.md`](../spec/README.md)，项目术语见 [`CONTEXT.md`](../CONTEXT.md)。本工作区只记录版本状态和版本说明，不重复维护流程规则。

## 目录结构

- `versions/vN/index.html`：第 N 个演示版本的单文件报告快照，自包含、可离线打开。
- `versions/vN/NOTES.md`：该版本的元数据、设计意图、优化记录、验收结果和遗留项。

无需安装依赖或执行构建，直接用浏览器打开对应的 `index.html` 即可预览。版本之间不共享外部资源，每个 `index.html` 独立可分享。

## 版本索引

| 版本 | 文件 | 内容基线 | 包含章节 | 快照状态 | 说明 |
| --- | --- | --- | --- | --- | --- |
| v1 | [index.html](versions/v1/index.html) | 无 | 无 | 未同步 | 逐页演示与打印降级骨架，尚未同步任何已确认正文 |

- 当前交付版本：暂无（v1 仅为骨架，未进入已验收）。
- 生成新版本时在表格追加一行，并同步更新本节的当前交付版本。

## 版本目录模板

新版本从上一版本目录复制而来；`NOTES.md` 使用以下结构：

```markdown
# vN

## 版本元数据

| 项目 | 值 |
| --- | --- |
| 内容基线 | <确认提交 hash 或 “无”> |
| 包含章节 | <NN、NN 或 “无”> |
| 快照状态 | 未同步 / 已同步 / 已验收 |
| 来源版本 | v(N-1) 或 — |

## 本版本意图

一句话说明这一版相对上一版要解决什么。

## 优化记录

| 轮次 | 改动 | 触发原因 | 是否更换内容基线 |
| --- | --- | --- | --- |
| 1 | 初始生成 | — | 否 |

## 验收检查

对应 `spec/README.md` 展示网页验收项 1–6。

| 序号 | 检查项 | 结果 |
| --- | --- | --- |
| 1 | 章节与基线一致 | 待检查 |
| 2 | 离线打开、目录与逐页导航 | 待检查 |
| 3 | 16:9 无溢出或信息墙 | 待检查 |
| 4 | 打印分页与静态降级 | 待检查 |
| 5 | 无待填写或调试标记 | 待检查 |
| 6 | 署名与高风险信息标记 | 待检查 |

## 遗留项

- 待补充
```
