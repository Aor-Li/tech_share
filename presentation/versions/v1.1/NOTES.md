# v1.1

## 版本元数据

| 项目 | 值 |
| --- | --- |
| 内容基线 | `7b1577d` |
| 包含章节 | 01、02、03 |
| 快照状态 | 未同步 |
| 来源版本 | v1 |

## 本版本意图

v1 的内容和图示结构已经合格，问题在观感：整份报告是浅色底 + 深色页混排，版面留白偏多，若干张 SVG 上有被裁掉或叠在一起的文字。这一版**只改设计与排版，不动信息结构**——换成 Catppuccin Mocha 深色主题，修掉逐页量测出来的显示缺陷，并把图示浪费掉的纵向空间还给正文。43 页构成、36 张图示的内容与页序保持不变（第 5 轮另删去导览页，现为 42 页、35 张图示）。

## 优化记录

| 轮次 | 改动 | 触发原因 | 是否更换内容基线 |
| --- | --- | --- | --- |
| 1 | 从 v1 复制 | 建立 v1.1 工作副本 | 否 |
| 2 | 全站配色改为 Catppuccin Mocha；打印降级为 Catppuccin Latte | 用户要求调整设计并使用 Mocha 主题 | 否 |
| 3 | 修复 SVG 越界裁切、文字互相压盖、第 42 页流程框错位等 12 处显示缺陷 | 逐页量测发现 | 否 |
| 4 | 图示改为按自身比例取高，正文基准字号 14.5px → 16.3px；封面与章节页重排 | 消除图示上下的空带与版面留白 | 否 |
| 5 | 删除第 2 页「全篇地图」，43 页 → 42 页，36 张图示 → 35 张 | 图中「收束 / 五步闭环」是 outline 00 的未确认遗留，去掉后该页信息量过小，不值单独一页 | 否 |
| 6 | 第 05 页 Attention 数据流图重绘：补齐缺失箭头、连线止于框边、QKV 扇出改为共用干线，合计框从右上角移到图底通栏；页脚 `h` 补注为 head 数 | 图中三处连线无箭头且一条画进框内，合计框挤占右上角 | 否 |

> 本文件其余章节的页码沿用删页前的 43 页编号；第 5 轮删掉第 2 页后，原第 3 页及之后各页页码减 1。

## 配色

Catppuccin Mocha，语义变量全部集中在 `:root`，35 张 SVG 通过 `var(--accent)` 等变量取色，因此换色不需要改任何图示标记。

| 用途 | 取色 | 色值 |
| --- | --- | --- |
| 页面底色 | Base | `#1e1e2e` |
| 深色页（封面 / 章节页 / 收束页） | Crust | `#11111b` |
| 正文 / 次级文字 | Text / Subtext0 | `#cdd6f4` / `#a6adc8` |
| 分隔线 / 强分隔线 | Surface0 / Surface2 | `#313244` / `#585b70` |
| 01 与开场收尾主色 | Teal | `#94e2d5` |
| 02 主色 | Blue | `#89b4fa` |
| 03 主色 | Mauve | `#cba6f7` |
| 代价 / 警示色（全篇统一） | Peach | `#fab387` |

每章一个色相由 `.slide[data-ch^="02"]` 一类选择器切换 `--accent`，现场翻页时能直接从色相判断当前在哪一章；表示「代价、副作用」的 Peach 不随章节变化，保持全篇一致的语义。

打印时整套变量切换到 Catppuccin **Latte**（白底 `#fff`、正文 `#4c4f69`、主色 `#179299` / `#1e66f5` / `#8839ef`），解决 v1 遗留的「深色页打印大量着色」问题。

## 排版调整

| 项目 | v1 | v1.1 | 说明 |
| --- | --- | --- | --- |
| 正文基准字号（1280×720） | 14.5px | 16.3px | `clamp(14px,2.26vh,21px)` |
| 图示取高方式 | `height:100%` + `meet` | `height:auto` 取 viewBox 比例 | 见下 |
| 内容块对齐 | 顶对齐 | `.body` 垂直居中 | 图示与配套卡片作为一组居中 |
| 卡片 | 仅描边 | 描边 + `--panel` 底色 | 深色底上纯描边卡片过弱 |
| 表格 | 表头灰字 | 表头用章节主色 + 偶数行浅底 | 深色底上便于横向读行 |

**图示留白**：v1 里 SVG 以 `height:100%` 铺满容器，而这些图的 viewBox 都偏扁（多为 1000×250 ~ 1000×350），`preserveAspectRatio="meet"` 会在上下各留一条空带。逐页量测的空带高度从 31px 到 244px 不等，中位数约 120px——约占图示区的四分之一。改成 `height:auto` 后图示按自身比例取高，空带归零，省下的纵向空间用于放大正文。

## 修复的显示缺陷

以下 12 处由脚本逐页量测（元素越出 `.frame`、图形越出 viewBox、SVG 内文字包围盒相交、细线横穿文字、描边框互相重叠）后定位，均在 v1 中已存在：

| 页 | 问题 | 处理 |
| ---: | --- | --- |
| 02 | 顶部虚线注解压在「02 训练」框上 | viewBox 上扩 46 单位，注解移到框线以上 |
| 09 | 6P 条形的「403 TFLOPs · Qwen3-32B」越过中缝压住右栏 | 模型名移入条形内，数值留在条外 |
| 09 | 「H3 · 20B 激活」与右侧数值贴死 | 左栏已写明口径，此处简化为「H3 · 20B」 |
| 19 | 「a · gradient accumulation」与释义贴死 | 缩短为「a · grad accumulation」 |
| 21 | 「付出：…」整句被 viewBox 右边界切掉 36 单位 | 两个说明框内文字改为两行排 |
| 21 | 「forward」「backward 之前」越出右边界 | 改为右对齐；「backward 之前」拆两行，与「重算 L8」拉开间距 |
| 24 | ZeRO 三行说明越过中缝压住 FSDP 面板 | 说明按「·」折成两行，中缝右移到 654 |
| 26 | 行底纹与右侧标注越出右边界 4 单位 | viewBox 宽度 1000 → 1008 |
| 27 | 底部两行说明被下边界切掉 10 单位 | viewBox 高度 300 → 316 |
| 28 | 「节拍由 E0 决定」压住「结论」框 | 改为右对齐并上移到分隔线顶端 |
| 33 | KV 说明整行压住「主要影响」框 | 移到左侧作为独立说明行 |
| 37 | 行标「D / E」越出左边界 24 单位 | viewBox 左扩到 -30 |
| 42 | **第 5 个流程框错位 40 单位**，第 4→5 的箭头画进框内、横穿「验证」二字 | 框位由 812 校正到 852（与前四框同为 208 间距），viewBox 宽度扩到 1040，回环虚线改从新框中心起 |

## 内容改动

设计改版原则上不动正文，仅以下三处为消除压字而改写措辞，语义不变：

- 第 21 页「换来 / 付出」两段折行（文字未删改）。
- 第 24 页 ZeRO-3 说明「之后再次分片或释放」→「之后再分片或释放」。
- 第 09 页右栏「H3 · 20B 激活」→「H3 · 20B」；第 19 页「gradient accumulation」→「grad accumulation」。

## 新增

章节页（03 / 17 / 31 页）右侧加入「本章路线」小节列表。这三页原先只有一个大号章节号加一句导语，正文区有近 400px 空白；改为左右分栏后，右栏列出本章 5 / 9 / 6 个小节，翻到章节页时能同时看到位置与路线。列表为静态 HTML，不依赖 JS。

## 验收检查

对应 `spec/README.md` 展示网页验收项 1–6。

| 序号 | 检查项 | 结果 |
| --- | --- | --- |
| 1 | 章节与基线一致 | 不通过：与 v1 相同，正文尚未确认，文件头仍记为 `outdated` / 未同步 |
| 2 | 离线打开、目录与逐页导航 | 通过：单文件、无外部依赖；目录按钮与 `M` 打开分章目录（章节标题按章色区分），方向键 / 空格 / PageUp / PageDown / Home / End 翻页，`Esc` 关闭 |
| 3 | 16:9 无溢出或信息墙 | 通过：1280×720 下脚本逐页量测 42 页，元素越出画布 0 处、图形越出 viewBox 0 处、SVG 内文字相交 0 处、间距小于 6 单位的文字对 0 处 |
| 4 | 打印分页与静态降级 | 通过：A4 横向下 42 页各占一页；打印整套变量切到 Latte 浅色，深色页不再满版着色；35 张图示仍为静态内联 SVG |
| 5 | 无待填写或调试标记 | 通过 |
| 6 | 署名与高风险信息标记 | 通过：沿用 v1 的 `FACT-` / `REF-` 页脚编号与口径标注 |

## 遗留项

- 三章正文确认后重新核对文字，再把文件头与版本索引推进到 `已同步`。
- 第 08 页（删页前编号 09）两组条形用的是两套比例尺（前向 2P 组 2.22 px/TFLOP，训练 6P 组 0.74 px/TFLOP），403 TFLOPs 的条与 134 TFLOPs 的条画得一样长。这是 v1 就有的数据编码问题，属于图表语义而非样式，本版未动；下一轮建议改成同一比例尺或明确标出两组不可横向比较。
- 02 与 03 的深色收束页信息量仍偏高，若按 30 分钟讲需要决定跳过还是拆页（沿用 v1 遗留项）。
- 章节页的「本章路线」目前是手写列表，与 `data-title` 不联动；若后续增删页面需要手工同步。
