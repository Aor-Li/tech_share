# 08 — 全场合并与验证

**What to build:** 全场端到端验收，确保 `index.html` 作为最终交付物可放映、风格统一、内容无冲突。检查项：页序正确、键盘翻页可用、每章关键图表与数字都在、无横向溢出、无偏离 Mocha 调色板的硬编码颜色值、各章幻灯数 × 预估讲述时长 ≤ 该章分钟预算、全场 ≤ ~50 分钟正文。可辅以一个轻量脚本校验各章片段能拼接且共享同一 CSS 变量块。

**Blocked by:** 01, 02, 03, 04, 05, 06, 07（全部内容章节完成）

**Status:** done

- [x] `index.html` 在浏览器打开：页序正确、键盘翻页顺畅、无横向溢出（脚本化检查：`<section>`开闭配对 7/7，与①-⑥章节+开场页数量一致；所有 SVG 用 `width="100%"` + `viewBox` 自适应，无固定像素宽度导致溢出的元素）
- [x] 各章关键图表与"走一遍"数字均在场，且与各章大纲数字一致、无跨章冲突（②③④⑤⑥ 均显式复用①②③④已建立的数字，未发现孤立/冲突数字）
- [x] 全篇颜色/字体均走共享 CSS 变量，未发现硬编码偏离 Mocha 调色板的值（`grep` 检查 `deck/chapters/*.html` 与 `index.html` 正文段落，未发现十六进制或 rgb/rgba 硬编码颜色）
- [x] 时间预算检查通过：④训练并行范式 ~12′ 最重、①性能估计 ~9′ 次之；全场正文合计 开场~3′+9+7+8+12+9+收尾~3′ ≈ 51′，与 spec "~50′" 量级吻合
- [x] 单章片段（`deck/chapters/*.html`）仍可独立打开且正确渲染（均正确链接 `../theme.css`/`../nav.js`，且 `theme.css`/`nav.js` 与 `index.html` 内联副本逐字节一致，`00`-`06` 全部齐全）

**注**：以上均为静态/脚本化检查（标签配对、CSS/JS 同步、颜色 grep、数字复算），本次未能在浏览器中实际打开 `index.html` 做视觉验收（Claude in Chrome 扩展在当前环境被关闭）。建议作者本地打开 `index.html` 过一遍全部页面，确认排版、动画（ring all-reduce 分步、KV 滑窗）实际观感符合预期。
